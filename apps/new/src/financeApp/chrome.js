/**
 * @deprecated Prefer src/appShell — kept for any leftover imports.
 * Finance page chrome (tabs/header) now lives in financeApp/mount.js inside the app shell.
 */
export function renderFinanceChrome() {
    console.warn('[financeApp/chrome] deprecated — use appShell via mountFinancePage');
}
