/**
 * Resolve Evolyx passbook config from Mongo (integrations domain).
 */
export {
    EVOLYX_PROVIDER,
    EVOLYX_PASSBOOK_KEY,
    resolveEvolyxPassbookConfig,
    requireEvolyxPassbookConfig,
} from './integrationsMongo/connectionsStore.js';
